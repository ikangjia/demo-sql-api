server:
  port: 9002

spring:
  application:
    name: demo-sql-api
  profiles:
    active: pc

mybatis-plus:
  global-config:
    banner: off
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

logging:
  level:
    cn.ikangjia.demo.domain.mapper: debug